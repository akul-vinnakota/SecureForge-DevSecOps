[project]
name = "secureforge"
version = "0.1.0"
description = "A DevSecOps cloud security pipeline for detecting security issues before deployment."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]

[tool.bandit]
exclude_dirs = ["tests"]
